# GSA Plus API
Microservices-based backend API for the GSA Plus application

# Environment Variables
- `POSTGRES_USR` - username for Postgres DB
- `POSTGRES_PSW` - password for Postgres DB

# Jenkins Credentials
- `gsaplus_db` - username/password style credential for Postgres DB. Auto populates above environment variables
